# User Prompt
```
process files recursively
```

# Walkthrough - Recursive File Processing

I have enabled recursive file processing in the file renamer Android application.

## Changes

### Android Application

#### [MainActivity.java](file:///x:/Projects_X/0_Active/8_Android_APK/02_Stable/Randomizer_APK_GITHUB/app/src/main/java/com/vypeensoft/randomizer/MainActivity.java)
- Extracted original file processing logic to a dedicated helper method `processFile(...)`.
- Added a `ProcessStats` helper class to keep track of successful, failed, and skipped files during the background action execution.
- Added `processDirectoryRecursively(...)` to recursively walk directory structures.
- Updated `startAction(...)` to invoke the recursive directory walker on the user's selected/configured folder paths.

## Verification

- The changes compile cleanly.
- The recursive walking structure ensures nested folders and files are automatically processed for all three main actions (Randomize, Reverse, Checksum).
